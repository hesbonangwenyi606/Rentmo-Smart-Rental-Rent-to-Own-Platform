// This file is unused in the Vite build — App.tsx handles layout via BrowserRouter.
// Kept as a placeholder to avoid import errors.
export default function RootLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
